# Saved visualizations / 已保存的可视化

Generated HTML files are saved here. 生成出来的 HTML 可视化文件会保存在这里。

For a public teaching repository, keep only a few selected examples, such as three short demos from different subject areas. Avoid using examples that make the project look limited to one discipline. 如果要公开上传到 GitHub，建议只保留少量精选示例，例如三个来自不同领域的短 demo。尽量避免让示例看起来只属于某一个专业。
